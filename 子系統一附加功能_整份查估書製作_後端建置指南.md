# 子系統一附加功能：整份查估書製作後端建置指南

> 所屬子系統：子系統一「估價書輔助製作系統」  
> 功能定位：在既有案件、宗地、表單、因素、比較標的及文件功能上，增加整份查估書的引導製作、檢核、組版及 PDF 輸出。  
> 競賽 MVP：依附件所示的商業用地比較法範本，產生一份完整六頁查估書。  
> 資料庫原則：優先重用現有資料表，不新增專用資料表。

---

## 1. 文件目的

本文件是子系統一後端建置指南的附加功能說明，目標不是只產生單一查估表，而是讓估價人員在 AI 助理引導下完成一整份查估書。

後端應支援：

- 分析整份查估書需要的案件資料、表單、因素、比較案例及附圖。
- 引導使用者上傳必要文件或補充無法自動取得的資料。
- 自動帶入案件、宗地、比準地及比較標的基本資料。
- 製作地價區段勘查表。
- 製作影響地價區域因素分析明細表。
- 製作比較法調查估價表。
- 整合地價區段略圖、使用分區圖及地價區段圖。
- 由後端執行等級、修正率及比較法計算。
- 執行整份查估書的完整性、格式、範圍、公式及跨頁一致性檢核。
- 將各頁依固定順序組版、合併並產生完整 PDF。
- 將 PDF 寫入 MinIO，並在 PostgreSQL 保存文件 metadata 及操作事件。

本功能的核心價值是：

> 使用者不必在多張表格之間重複抄填，系統可以重用同一份案件與因素資料，自動完成跨表帶入、計算、檢核及整份輸出。

---

## 2. 整份查估書的 MVP 範圍

附件範本共六頁：

| 頁次 | 頁面代碼 | 內容 | 後端來源 |
|---:|---|---|---|
| 1 | `S01` | 地價區段勘查表 | 案件、宗地、勘查資料、因素資料、使用者確認值 |
| 2 | `F02-RF` | 影響地價區域因素分析明細表（商業用地） | 比準地、比較標的、因素定義、等級、權數及修正率 |
| 3 | `F02` | 比較法調查估價表 | 比較案例、交易日期、區域／個別因素及價格計算 |
| 4 | `MAP-01` | 地價區段略圖 | 使用者上傳的圖檔及案件標示資訊 |
| 5 | `MAP-02` | 地價使用分區圖 | 使用者上傳的圖檔及使用分區、圖例資訊 |
| 6 | `MAP-03` | 地價區段圖 | 使用者上傳的圖檔及地價區段標示資訊 |

競賽階段先固定支援：

```text
土地用途：商業用地
估價方法：比較法
比準地：1 筆
比較標的：1～3 筆
輸出格式：固定六頁 A4 PDF
附圖方式：上傳既有圖片或 PDF 後嵌入
```

暫不要求所有縣市、所有土地用途、收益法、成本法或 GIS 自動繪圖。

---

## 3. 完整使用流程

```text
估價人員登入
→ 建立新案件或進入既有案件
→ 選擇「商業用地比較法完整查估書」
→ AI 助理取得整份查估書需求清單
→ 檢查案件、宗地、表單、比準地、比較標的及附圖是否存在
→ AI 逐項引導上傳必要文件或填寫缺少資料
→ 系統擷取文件內容並建立候選欄位
→ 使用者確認或修正候選欄位
→ 製作地價區段勘查表
→ 計算或確認最近設施的最短步行距離
→ 依正式評價基準提出因素等級候選
→ 使用者確認區域因素及個別因素等級
→ 後端計算因素修正率及小計
→ 完成影響地價區域因素分析明細表
→ 後端執行交易日期、區域因素、個別因素及價格計算
→ 完成比較法調查估價表
→ 上傳三張附圖
→ 系統執行整份查估書檢核
→ 使用者依提示修正資料
→ 再次檢核通過
→ AI 助理呼叫完整查估書產生工具
→ 後端渲染六頁並合併 PDF
→ 上傳 MinIO generated 路徑
→ 寫入 documents metadata 與案件事件
→ 提供完整 PDF 下載
```

---

## 4. 功能與既有資料表的對應

原則上重用現有 PostgreSQL `valuation` schema：

| 功能 | 既有資料表 | 用途 |
|---|---|---|
| 案件資料 | `cases` | 案號、地區、估價基準日、案件狀態及建立人 |
| 宗地資料 | `parcels` | 地段、地號、面積及宗地資料 |
| 通用表單 | `form_instances` | 保存 `S01`、`F02-RF`、`F02` 的草稿、狀態及 JSONB 表單內容 |
| 比準地 | `benchmark_lands` | 比準地地號、所屬區段及基本資料 |
| 比準地評價 | `benchmark_valuations` | 比準地估價與已確認的評價結果 |
| 比較標的 | `comparison_targets` | 比較案例基本資料與順序 |
| 因素定義 | `factor_definitions` | 主要項目、修正細項、因素代碼與排序 |
| 因素級距 | `factor_levels` | 用地類型、級距、優劣等級、權數及修正率依據 |
| 比較因素值 | `comparison_factor_values` | 比準地與比較標的的原始值、等級、權數及修正結果 |
| 比較分析 | `comparison_analyses` | 各標的因素小計、總修正率及比較法分析結果 |
| 宗地估價 | `parcel_valuations`、`parcel_valuation_items` | 宗地最終估價及計算項目 |
| 文件 metadata | `documents` | 原始文件、附圖及產出 PDF 的 bucket、object key、版本及雜湊 |
| 規則版本 | `rule_versions` | 使用的公式、因素級距及評價基準版本 |
| 修改紀錄 | `change_logs` | 修改前後內容、操作者、時間及原因 |

### 4.1 不新增資料表的前提

必須先確認：

- `form_instances` 有 JSONB 或等效的完整表單內容欄位。
- `form_type` 可新增 `S01`、`F02-RF`，或沒有只允許 F01～F04 的限制。
- `factor_definitions` 能表示主要項目、修正細項與排序。
- `factor_levels` 能區分土地用途、規則版本及有效期間。
- `comparison_factor_values` 能明確區分比準地與比較標的，或能從關聯來源判斷。
- `documents` 的 category 可保存三種附圖與 generated PDF。

若既有 CHECK constraint 或 Enum 不允許新代碼，仍然不必新增資料表，但需用 Alembic migration 擴充既有限制。

---

## 5. 查估書套件定義

整份查估書不需要另外建立資料表，可先用程式設定檔定義：

```python
REPORT_PACKAGES = {
    "REPORT_COMPARISON_COMMERCIAL": {
        "name": "商業用地比較法完整查估書",
        "land_use_type": "COMMERCIAL",
        "valuation_method": "COMPARISON",
        "pages": [
            "S01",
            "F02-RF",
            "F02",
            "MAP-01",
            "MAP-02",
            "MAP-03",
        ],
    }
}
```

頁面完成度由後端依既有資料計算，不需要建立新的 package table。

### 5.1 建議表單及文件代碼

```text
S01       地價區段勘查表
F02-RF    比較法－區域因素分析明細表
F02       比較法調查估價表
MAP-01    地價區段略圖
MAP-02    地價使用分區圖
MAP-03    地價區段圖
REPORT    完整查估書 PDF
```

---

## 6. 地價區段勘查表 `S01`

### 6.1 資料來源

可直接帶入：

- 案號。
- 縣市、行政區、地段及地號。
- 估價基準日。
- 地價區段號。
- 都市計畫內外。
- 使用分區。
- 建蔽率及容積率。
- 比準地及比較標的資料。

需由文件擷取或人工確認：

- 地價區段範圍。
- 道路條件。
- 公共設施及生活機能。
- 商業繁榮程度。
- 嫌惡設施及環境條件。
- 發展趨勢。
- 各設施位置及距離。
- 備註與現場勘查意見。

### 6.2 建議 JSONB 結構

```json
{
  "form_type": "S01",
  "header": {
    "case_number": "1140901-99-001",
    "valuation_date": "2025-09-01",
    "district_code": "P002-00",
    "district_name": "新北市金山區",
    "district_boundary": "區段範圍文字",
    "survey_date": "2025-09-18"
  },
  "factors": [
    {
      "factor_code": "PUBLIC_FACILITY.PARKING",
      "raw_value": "金包里老街停車場",
      "selected_level": "NEAR",
      "source_type": "MAP_ROUTE",
      "source_document_id": null,
      "confirmed_by_user": true
    }
  ],
  "notes": "",
  "status": "DRAFT"
}
```

---

## 7. 最近設施與基本距離

本功能中的「基本距離」統一定義為：

> 從指定起算點，沿可實際步行道路前往同類型設施中最近一處設施的最短步行距離，不使用經緯度直線距離。

### 7.1 起算點

每個案件必須明確選定：

```text
SUBJECT_PARCEL_ENTRANCE     勘估標的主要出入口
BENCHMARK_LAND_ENTRANCE     比準地主要出入口
DISTRICT_REPRESENTATIVE     地價區段指定代表點
MANUAL_COORDINATE           人工指定座標
```

MVP 建議使用比準地或勘估標的主要出入口；若無出入口座標，允許使用宗地中心點，但必須標示資料品質較低並請使用者確認。

### 7.2 最近設施計算

```text
取得起算點
→ 依設施類型搜尋候選設施
→ 對每個候選設施取得步行路線距離
→ 排除不可步行或路線失敗的候選
→ 選擇 walking_distance_m 最小者
→ 保存設施名稱、距離、來源及查詢時間
→ 對照 factor_levels 距離級距
→ 產生優劣等級候選
→ 使用者確認
```

### 7.3 建議保存內容

可保存於 `form_instances` JSONB 或 `comparison_factor_values` 的來源快照：

```json
{
  "facility_type": "PARKING_LOT",
  "facility_name": "金包里老街停車場",
  "distance_type": "WALKING",
  "walking_distance_m": "120",
  "origin_type": "BENCHMARK_LAND_ENTRANCE",
  "origin_reference_id": "uuid",
  "origin_latitude": "25.000000",
  "origin_longitude": "121.000000",
  "destination_latitude": "25.001000",
  "destination_longitude": "121.001000",
  "provider": "configured-map-provider",
  "route_reference": "provider-result-id",
  "measured_at": "2026-08-26T10:00:00Z",
  "confirmed_by_user": true
}
```

不得保存會過期的完整查詢 URL 或 API Key。

### 7.4 無地圖服務時的降級方式

開發期間若沒有可用地圖 API：

- 由使用者輸入設施名稱及最短步行距離。
- 系統要求標示資料來源。
- 允許上傳步行路線截圖作為附件。
- 保存 `source_type = MANUAL_CONFIRMED`。
- AI 可協助檢查數字格式，但不可假裝已自動查詢。

---

## 8. 區域因素分析明細表 `F02-RF`

### 8.1 表格內容

- 主要項目。
- 修正細項。
- 比準地權數及優劣等級。
- 比較標的一至三的權數、優劣等級及修正百分比。
- 各主要項目百分比小計。
- 其他影響因素。
- 區域因素修正率。
- 總修正率。
- 備註與不動產估價師欄位。

### 8.2 因素階層

建議重用 `factor_definitions`：

```text
土地使用管制
├── 都市計畫內外
├── 使用分區
├── 建蔽率
├── 容積率
├── 禁止建築
└── 限制建築

交通運輸
├── 主要道路寬度
├── 區段內道路平均寬度
├── 大眾運輸接近程度
└── 交流道接近程度

自然條件
公共設施
特殊設施
環境污染
工商活動
其他影響因素
```

若現有表沒有 `parent_factor_id`，可用 `factor_group`、`factor_code` 及 `sort_order` 表示階層，不必新增資料表。

### 8.3 等級確認

```text
讀取勘查表及來源證據
→ 依 land_use_type 與 rule_version 取得 factor_levels
→ AI 提出等級候選及來源說明
→ 使用者確認或修改
→ 保存 reported／confirmed level
→ 後端計算修正百分比
```

AI 只提出候選；正式權數、級距、修正率及小計由後端規則服務執行。

### 8.4 多個比較標的

不得設計成固定的：

```text
comparison_1_value
comparison_2_value
comparison_3_value
```

應使用既有 `comparison_targets` 一對多關係，PDF 模板依 `display_order` 取前一至三筆；超過三筆時後續版本再支援換頁。

---

## 9. 比較法調查估價表 `F02`

### 9.1 應自動帶入

- 案號及估價基準日。
- 比準地與比較標的地段、地號。
- 比較標的交易日期及正常單價。
- 地價區段號。
- 面積、寬度、深度、形狀及臨路條件。
- `F02-RF` 已確認的區域因素總修正率。
- 個別因素分析結果。

### 9.2 後端計算

- 交易日期調整。
- 區域因素調整。
- 個別因素調整。
- 其他調整。
- 調整後單價。
- 各比較標的試算價格。
- 最終比準價格或建議價格。

所有金額、比例、面積及修正率使用 PostgreSQL `numeric` 與 Python `Decimal`。

公式須：

- 綁定 `rule_version_id`。
- 保存輸入快照。
- 保存中間結果。
- 統一四捨五入規則及時點。
- 同一輸入與規則版本得到相同結果。
- 資料修改後產生新結果，不覆蓋舊版本。

本指南不從範本畫面反推正式修正公式；實作前必須依正式法規與地方評價基準確認。

---

## 10. 三張附圖

### 10.1 MVP 的處理方式

使用者上傳已製作完成的：

- `MAP-01`：地價區段略圖。
- `MAP-02`：地價使用分區圖。
- `MAP-03`：地價區段圖。

支援格式：

```text
PNG
JPEG
PDF
```

檔案內容放 MinIO；`documents` 保存：

- `case_id`
- `category`
- `document_group_id`
- `version_no`
- `mime_type`
- `file_size_bytes`
- `checksum_sha256`
- `bucket_name`
- `object_key`
- `uploaded_by_user_id`
- `is_active`

### 10.2 PDF 組版

模板應：

- 保持圖片比例，不任意拉伸。
- 超出框線時等比例縮放。
- 加上固定標題、比例尺文字、區段範圍、比準地及比較標的說明。
- 標示圖檔來源與製作日期。
- 檢查解析度，避免輸出模糊。

### 10.3 競賽階段不做的 GIS 功能

- 自動繪製地籍界線。
- 自動計算地價區段範圍。
- 自動疊加使用分區圖層。
- 自動產生比例尺。
- 自動完成座標轉換。
- 自動繪製道路、圖例及行政界線。

上述功能若未來要做，可能需要 GIS 圖資、PostGIS 或外部地圖服務，不屬於本次「不新增資料表」的一週 MVP。

---

## 11. AI 助理的責任

### 11.1 AI 可以做

- 取得完整查估書需求與目前完成度。
- 逐項詢問缺少資料。
- 協助分類上傳文件及三張附圖。
- 從文件擷取地段、地號、面積、使用分區及設施資訊。
- 根據正式因素級距提出優劣等級候選。
- 顯示候選結果的來源、理由與信心。
- 說明缺漏、跨表不一致及後端計算結果。
- 呼叫儲存草稿、計算、檢核及 PDF 產生工具。

### 11.2 AI 不可做

- 不可發明地號、面積、設施、距離或法規。
- 不可以直線距離冒充步行距離。
- 不可自行確認專業因素等級。
- 不可直接計算正式修正率與價格。
- 不可修改 `rule_versions`、`factor_levels` 或正式計算結果。
- 不可在缺少必要頁面或存在 `ERROR` 時產生正式完成版。
- 不可自行繪製正式 GIS 區段界線。

### 11.3 建議 AI 工具

| 工具 | 用途 | 是否需確認 |
|---|---|---:|
| `get_report_requirements` | 取得六頁需求 | 否 |
| `get_report_progress` | 取得完成度與缺少項目 | 否 |
| `get_case_summary` | 讀取案件基本資料 | 否 |
| `get_survey_candidates` | 取得勘查候選值 | 否 |
| `get_nearest_facility` | 取得最近設施最短步行距離 | 是 |
| `apply_confirmed_survey_fields` | 寫入確認後勘查值 | 是 |
| `get_factor_level_candidates` | 取得優劣等級候選 | 否 |
| `confirm_factor_levels` | 保存人工確認等級 | 是 |
| `run_factor_calculation` | 計算修正率 | 是 |
| `run_comparison_calculation` | 執行比較法計算 | 是 |
| `validate_complete_report` | 檢核整份查估書 | 否 |
| `generate_complete_report_pdf` | 產生完整 PDF | 是 |

模型不得直接操作 PostgreSQL 或 MinIO，所有動作都必須經 FastAPI Service 再次驗證 JWT、RBAC、`case_id` 及案件狀態。

---

## 12. 建議後端模組

```text
app/
├── valuation/
│   ├── report_packages/
│   │   ├── router.py
│   │   ├── schemas.py
│   │   ├── service.py
│   │   ├── requirements.py
│   │   └── templates.py
│   ├── surveys/                 S01 地價區段勘查表
│   ├── facilities/              最近設施及步行距離
│   ├── factors/                 因素定義、級距與等級候選
│   ├── comparison_analyses/     F02-RF
│   ├── calculations/            F02 與修正率計算
│   ├── validations/             單頁及整份檢核
│   ├── maps/                    三張附圖檢查與組版資料
│   └── reports/
│       ├── renderer.py
│       ├── merger.py
│       └── templates/
│           └── comparison_commercial/
│               ├── page_01_survey.html
│               ├── page_02_regional_factors.html
│               ├── page_03_comparison.html
│               ├── page_04_section_sketch.html
│               ├── page_05_zoning_map.html
│               └── page_06_land_value_section_map.html
├── ai_assistant/
├── storage/
├── auth/
├── core/
└── db/
```

此處是程式模組拆分，不代表新增資料表。

---

## 13. 核心 API

### 13.1 查估書類型與需求

```http
GET /api/v1/valuation/report-types
GET /api/v1/valuation/report-types/{report_type}/requirements
```

MVP report type：

```text
REPORT_COMPARISON_COMMERCIAL
```

### 13.2 建立或開啟查估書

```http
POST /api/v1/valuation/cases/{case_id}/reports
GET  /api/v1/valuation/cases/{case_id}/reports/{report_id}
```

若不新增 report table，`report_id` 可使用代表本次產出的 `form_instances` 主表單 ID，或由 `case_id + report_type + version` 在 Service 層組合；不可只在前端保存狀態。

### 13.3 完成度

```http
GET /api/v1/valuation/cases/{case_id}/report-progress
```

```json
{
  "report_type": "REPORT_COMPARISON_COMMERCIAL",
  "completion_rate": "83.33",
  "sections": [
    {"code": "S01", "status": "COMPLETED"},
    {"code": "F02-RF", "status": "COMPLETED"},
    {"code": "F02", "status": "COMPLETED"},
    {"code": "MAP-01", "status": "COMPLETED"},
    {"code": "MAP-02", "status": "MISSING"},
    {"code": "MAP-03", "status": "COMPLETED"}
  ],
  "blocking_errors": ["MAP-02_MISSING"]
}
```

### 13.4 勘查與距離

```http
GET   /api/v1/valuation/cases/{case_id}/surveys/S01
PATCH /api/v1/valuation/cases/{case_id}/surveys/S01
POST  /api/v1/valuation/cases/{case_id}/facilities/nearest
POST  /api/v1/valuation/cases/{case_id}/surveys/S01/confirm
```

### 13.5 因素分析與比較法計算

```http
GET   /api/v1/valuation/cases/{case_id}/comparison-analyses
PUT   /api/v1/valuation/cases/{case_id}/comparison-analyses/{analysis_id}/factors
POST  /api/v1/valuation/cases/{case_id}/comparison-analyses/{analysis_id}/calculate
POST  /api/v1/valuation/cases/{case_id}/comparison-calculations
GET   /api/v1/valuation/cases/{case_id}/comparison-calculations/{calculation_id}
```

### 13.6 附圖

重用現有文件上傳 API，category 使用：

```text
map-section-sketch
map-zoning
map-land-value-section
```

若現有 category 有資料庫限制，可暫時使用 `attachments` 並在 metadata／form JSONB 記錄用途；正式版本再以 Alembic 擴充允許類型。

### 13.7 整份檢核與產出

```http
POST /api/v1/valuation/cases/{case_id}/reports/validate
POST /api/v1/valuation/cases/{case_id}/reports/generate
GET  /api/v1/valuation/cases/{case_id}/reports/{document_id}/download
```

---

## 14. 整份查估書檢核

### 14.1 完整性

- `S01` 是否存在並完成。
- `F02-RF` 是否存在並完成。
- `F02` 是否存在並完成。
- 三張附圖是否齊全且可讀取。
- 比準地是否存在。
- 比較標的是否至少一筆。
- 必要原始文件是否存在。

### 14.2 資料一致性

- 六頁案號一致。
- 估價基準日一致。
- 行政區、地段、地號一致。
- 地價區段號一致。
- 比準地與比較標的編號一致。
- `S01` 因素值與 `F02-RF` 使用值一致。
- `F02-RF` 總修正率與 `F02` 帶入值一致。
- 比較案例交易日期與原始資料一致。

### 14.3 距離與因素

- 距離單位為公尺。
- `distance_type = WALKING`。
- 具有設施名稱、起算點、來源及查詢時間。
- 最近設施結果已由使用者確認。
- 等級存在於目前 `rule_version` 的 `factor_levels`。
- AI 候選值未經確認時不得進入正式計算。

### 14.4 計算

- 權數、優劣等級及修正率完整。
- 小計與總計重新計算正確。
- 修正率未超出規則範圍。
- 交易日期、區域因素及個別因素計算正確。
- 計算輸入未在最後一次計算後被修改。
- 所有金額及比例使用 Decimal。

### 14.5 PDF

- 六頁依固定順序存在。
- A4 頁面方向及尺寸正確。
- 表格沒有被切斷或溢出。
- 圖片清楚且未變形。
- 中文字型正常嵌入。
- 案號、頁碼及標題正確。

存在 `ERROR` 時不得產生正式完成版；`WARNING` 可由使用者確認後產生，但必須保存確認事件。

---

## 15. PDF 產生流程

```text
驗證 JWT、RBAC、case_id 與案件狀態
→ 取得最新有效的 S01、F02-RF、F02
→ 取得最新有效的三張附圖
→ 取得最新計算結果及 rule_version
→ 再次執行整份檢核
→ 建立不可變的產出輸入快照
→ 依模板渲染第 1～6 頁
→ 將各頁轉為 PDF
→ 合併成單一 PDF
→ 重新開啟檢查頁數及檔案完整性
→ 計算 SHA-256、大小及 ETag
→ 上傳 MinIO generated 路徑
→ 寫入 documents metadata
→ 寫入 REPORT_GENERATED 事件
→ 回傳 document_id 及下載 API
```

MinIO 路徑：

```text
cases/{case_id}/generated/{document_group_id}/v{version_no}/{document_id}_complete_valuation_report.pdf
```

不得保存完整 MinIO URL 或過期 presigned URL。

---

## 16. 文件版本與重新產生

- 表單修改不覆蓋正式完成版 PDF。
- 重新產生時建立新的 `document_id` 與 `version_no`。
- `document_group_id` 保持相同，代表同一份邏輯查估書。
- 舊 PDF 保留並標記非目前有效版本。
- 保存新版本使用的表單版本、文件版本、計算 ID 及規則版本。
- 資料修改後，舊計算結果及舊 PDF 不得悄悄變更。

若現有 `documents` 已有版本欄位，不需新增資料表。

---

## 17. 一週競賽 MVP 工作安排

### Day 1：需求、資料對應與套件完成度

- 確認六頁所需欄位。
- 確認現有表及 constraint。
- 建立 `REPORT_COMPARISON_COMMERCIAL` 設定。
- 完成 requirements 與 progress API。
- 準備一組固定完整測試案件。

驗收：系統能列出六頁的完成／缺少狀態。

### Day 2：S01 與基本距離

- 完成地價區段勘查表讀寫。
- 帶入案件及宗地基本資料。
- 實作最近設施步行距離介面或人工降級流程。
- 完成因素候選值的使用者確認。

驗收：可完成一張 S01，距離明確標示為最近設施最短步行距離。

### Day 3：F02-RF 與修正率

- 載入商業用地因素、級距及排序。
- 支援一筆比準地及一至三筆比較標的。
- 保存因素等級與證據。
- 後端計算分項、小計及總修正率。

驗收：固定輸入可重複得到相同區域因素分析結果。

### Day 4：F02 與附圖

- 帶入比較案例及區域因素結果。
- 完成一條比較法計算。
- 支援三張附圖上傳、分類及預覽資料。
- 完成跨表基本一致性檢核。

驗收：三頁表格資料完成，三張附圖齊全。

### Day 5：完整 PDF 與端到端驗收

- 完成六頁固定模板。
- 完成整份檢核。
- 合併 PDF、上傳 MinIO、寫入 documents。
- 保存版本、輸入快照及事件。
- 完整展示流程演練。

驗收：產出與附件結構相同、可開啟下載的六頁 PDF。

### Day 6～7：緩衝

- 修正中文、表格、圖片及分頁問題。
- 補齊權限、錯誤處理與整合測試。
- 測試地圖服務或 AI 不可用時的人工流程。
- 不再新增大型功能。

---

## 18. MVP 必須完成

1. 商業用地比較法完整查估書類型。
2. 六頁需求與完成度 API。
3. S01 地價區段勘查表。
4. 最近設施最短步行距離或可驗證的人工輸入流程。
5. F02-RF 區域因素分析。
6. 一筆比準地及一至三筆比較標的。
7. 後端因素等級、修正率、小計及總計計算。
8. F02 比較法調查估價表。
9. 三張附圖上傳及嵌入。
10. 整份必填、範圍、公式與跨表一致性檢核。
11. 固定六頁 A4 PDF 產生及合併。
12. MinIO 保存與 documents metadata。
13. PDF 版本保留與案件事件。
14. 完整端到端驗收。

---

## 19. 有時間再完成

- 住宅、工業、農業等其他土地用途模板。
- 所有地方評價基準及因素級距。
- 超過三筆比較標的自動換頁。
- 自動 GIS 製圖。
- 自動產生比例尺與圖例。
- 地籍、使用分區及地價區段圖層套疊。
- 任意查估書 PDF 的自動版型辨識。
- 多種估價方法整份輸出。
- 完整視覺化地圖標註編輯器。
- 電子簽章及正式簽核流程。

---

## 20. 端到端驗收

1. 估價人員登入。
2. 建立或選擇案件。
3. 選擇商業用地比較法完整查估書。
4. 系統列出六頁及所需資料。
5. 使用者完成 S01 基本資料。
6. 系統或使用者輸入最近設施的最短步行距離。
7. 使用者確認因素等級。
8. 建立比準地及至少一筆比較標的。
9. 後端計算 F02-RF 修正率。
10. 後端完成 F02 比較法計算。
11. 使用者上傳 MAP-01～MAP-03。
12. 系統故意發現一項缺漏或不一致。
13. 使用者修正後重新檢核。
14. 六頁皆通過必要檢核。
15. 產生完整六頁 PDF。
16. 使用者下載並正常開啟。
17. 確認 PostgreSQL、MinIO、表單、計算、規則版本、文件版本及事件一致。

---

## 21. 每次提交前檢查

- [ ] 未新增功能重複的資料表。
- [ ] 已確認 `form_instances`、文件類型及 form type constraint。
- [ ] 所有 API 使用統一 Router 與 Pydantic Schema。
- [ ] 所有寫入都檢查 JWT、RBAC 與 `case_id`。
- [ ] 跨案件引用被拒絕。
- [ ] 金額、比例、面積、權數及距離使用 Decimal／numeric。
- [ ] 距離明確為最短步行距離，不是直線距離。
- [ ] 最近設施保存名稱、起點、來源及查詢時間。
- [ ] AI 候選值未確認前不進入正式計算。
- [ ] 正式修正率與價格由後端計算。
- [ ] S01、F02-RF 與 F02 跨表資料一致。
- [ ] 三張附圖齊全且版本正確。
- [ ] PDF 固定六頁、無裁切、無亂碼及圖片變形。
- [ ] 重新產生會建立新文件版本。
- [ ] MinIO 寫入失敗與 DB 寫入失敗有補償處理。
- [ ] `/health/ready` 仍正常。
- [ ] Swagger 可完成主要流程。
- [ ] 新增功能有單元及整合測試。

建議 Commit Message：

```text
feat(report): add complete valuation report package
feat(survey): add land value section survey workflow
feat(facilities): add nearest walking distance support
feat(factors): add commercial regional factor analysis
feat(calculation): add comparison method calculation
feat(report): add valuation map page composition
feat(report): generate six-page valuation report PDF
test(report): add complete valuation report workflow tests
docs(report): add complete report backend guide
```

---

## 22. 最終完成定義

此附加功能完成的標準是：

> 估價人員能在 AI 助理引導下完成商業用地比較法所需資料，製作地價區段勘查表、影響地價區域因素分析明細表及比較法調查估價表；系統能使用最近設施的最短步行距離作為距離資料，經使用者確認因素等級後，由後端計算修正率與比較法結果；使用者再上傳地價區段略圖、使用分區圖及地價區段圖，系統完成整份跨表檢核，最後產生、保存並提供一份完整六頁查估書 PDF。

在現有 `form_instances`、因素、比較標的、比較分析、估價、文件、規則版本及修改紀錄資料表可支援上述欄位的前提下，本功能不需要新增資料表；主要工作是新增表單定義、查估書套件設定、API、計算與檢核 Service、固定 PDF 模板及 AI 工具流程。

