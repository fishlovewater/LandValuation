# 估價書輔助製作系統：後端建置指南

> 適用範圍：第一個子系統「估價書輔助製作系統」  
> 開發時程：一週  
> 建置目標：以 AI 助理引導估價人員，完成資料準備、文件擷取、欄位確認、公式計算、製作前檢核及 PDF 估價書產出。

---

## 1. 文件目的

本文件提供第一個子系統的後端建置指南，協助開發人員快速掌握：

- 系統在一週內必須完成的競賽 MVP。
- AI 助理、FastAPI、PostgreSQL、MinIO 與文件辨識服務的分工。
- AI 如何從資料引導一路協助到 PDF 產出。
- 開發時必須遵守的資料、計算、權限及檔案規則。
- API 與服務的建置順序。
- 每個階段完成後的驗收方式。
- 哪些功能必須完成，哪些應延後。

本子系統不是單純的表單 CRUD，而是一個「由 AI 助理驅動、由後端規則保證正確性」的估價書製作流程。

---

## 2. 核心使用流程

```text
估價人員登入
→ 建立新案件或進入既有案件
→ 選擇要完成的查估表類型
→ AI 助理分析該表所需資料、必填欄位與必要文件
→ AI 助理逐項引導使用者上傳必要文件或填寫基本資料
→ AI 助理詢問是否有其他輔助資料需要上傳
→ 系統進行 PDF 文字擷取、OCR、文件分類與欄位辨識
→ AI 助理整理可直接帶入、需要確認及需要計算的欄位
→ 使用者確認或修正擷取結果
→ 後端將已確認資料寫入表單草稿
→ AI 助理呼叫後端公式工具完成估價計算
→ AI 助理解釋計算來源與尚未完成項目
→ AI 助理呼叫機器檢核工具
→ 顯示必填、缺件、格式、範圍、公式與跨表一致性問題
→ 使用者修正資料並重新檢核
→ 使用者確認資料正確
→ AI 助理呼叫 PDF 產生工具
→ 系統產生、保存並提供 PDF 電子版估價書下載
```

所有宗地、文件、表單、擷取結果、計算結果與 PDF 都必須先隸屬於同一個 `case_id`。

---

## 3. AI 助理的定位與技術原則

### 3.1 建議架構

競賽版本建議採用：

```text
Vue 對話式製作介面
        ↓
FastAPI AI Orchestrator
        ↓
Amazon Bedrock Converse API
        ↓ tool use
案件／需求分析／文件／表單／計算／檢核／PDF 工具
        ↓
PostgreSQL + MinIO
```

文件處理可採用：

```text
文字型 PDF → 本機 PDF 文字擷取
掃描 PDF／圖片 → Amazon Textract OCR／表格／表單分析
擷取文字與版面結果 → Bedrock 進行文件分類及欄位語意對應
```

若比賽環境尚未提供 Textract 或相關 AWS 權限，MVP 可先支援文字型 PDF，並保留 `DocumentExtractionProvider` 介面，之後再替換為 Textract。

### 3.2 AI 可以做什麼

- 根據表單類型整理必填欄位、必要文件與下一步。
- 以自然語言逐項詢問缺少的案件資料。
- 判斷上傳文件可能屬於土地登記、地籍圖、照片或其他附件。
- 將 OCR／PDF 擷取結果對應到系統欄位。
- 為候選欄位提供信心分數、來源頁碼及說明。
- 呼叫後端工具儲存草稿、計算、檢核及產生 PDF。
- 將後端檢核結果轉成容易理解的修正提示。
- 告知使用者目前完成度與下一個必要動作。

### 3.3 AI 不可直接決定什麼

- 不可自行發明地號、面積、價格、法規或比較案例。
- 不可將低信心擷取值直接寫入正式表單。
- 不可直接以語言模型計算正式地價、修正率或總價。
- 不可繞過 JWT、RBAC、案件狀態或跨案件檢查。
- 不可在未通過必要檢核時產生「正式完成」狀態的估價書。
- 不可將 AI 回答視為唯一事實來源。

### 3.4 正確的責任分工

| 工作 | 負責元件 |
|---|---|
| 對話引導與下一步建議 | Bedrock 模型 |
| 文件 OCR、表格與表單辨識 | Textract 或可替換的擷取服務 |
| 欄位語意對應 | Bedrock + 固定欄位 Schema |
| 寫入案件與表單 | FastAPI 工具與 Service |
| 金額、比例與修正率計算 | Deterministic Calculation Service |
| 必填、範圍及公式檢核 | Validation Service |
| 權限與案件範圍 | JWT、RBAC 與 Service |
| PDF 排版與輸出 | 固定模板 PDF Service |
| 結構化資料與歷程 | PostgreSQL |
| 原始文件與產出 PDF | MinIO |

對使用者而言，是由 AI 助理一路協助完成；對系統而言，每一個正式動作仍由可測試、可追蹤的後端工具執行。

---

## 4. 現有開發基礎

目前專案已完成：

- FastAPI 共用後端骨架。
- PostgreSQL 與 SQLAlchemy AsyncSession。
- Alembic migration 流程。
- MinIO 上傳、下載及刪除服務。
- JWT 登入。
- RBAC 角色與權限檢查。
- 統一錯誤格式及 request ID。
- `/health/live` 與 `/health/ready`。
- Docker Compose 啟動流程。

第一個子系統必須沿用上述共用服務，不得自行建立另一套資料庫連線、MinIO Client、登入系統或錯誤格式。

---

## 5. 系統資料如何存放

### 5.1 PostgreSQL

PostgreSQL 保存可以查詢、關聯、計算及追蹤的資料：

- 案件基本資料及案件狀態。
- 宗地、地段、地號及面積。
- F01～F04 表單及欄位資料。
- 表單需求定義及必要文件類型。
- 買賣實例及比較案例。
- 修正率、計算輸入快照、過程及結果。
- 文件 metadata 與 MinIO 路徑。
- 文件擷取工作與候選欄位。
- AI 對話工作階段、工具呼叫摘要及處理狀態。
- 機器檢核結果。
- 案件事件及欄位修改紀錄。

### 5.2 MinIO

MinIO 保存真正的檔案內容：

- 原始估價書 PDF。
- 土地登記資料。
- 地籍圖。
- 現場勘查照片。
- 其他案件附件。
- 系統產生的估價書及檢核報告。

原則：結構化資料放 PostgreSQL；PDF、圖片與附件放 MinIO。不可將大型檔案二進位內容寫入 PostgreSQL。

### 5.3 AI 處理資料

為了方便追蹤，至少應保存：

- `assistant_session_id`
- `case_id`
- `current_step`
- `selected_form_type`
- `missing_fields`
- `missing_documents`
- `last_tool_name`
- `last_tool_status`
- `model_id`
- `prompt_version`
- `created_at`、`updated_at`

競賽 MVP 不必永久保存完整思考過程。建議只保存使用者訊息、AI 最終回應、工具名稱、工具輸入摘要、工具結果摘要及 request ID，且不得將不必要的個資寫入日誌。

---

## 6. 開發時必須遵守的規則

### 6.1 資料庫結構由 Alembic 管理

- 正式資料庫結構以最新 Alembic revision 為準。
- FastAPI 不可呼叫 `Base.metadata.create_all()` 建表。
- 新增資料表、欄位或約束時建立新的 Alembic revision。
- 不回頭修改已合併及執行過的 migration。
- 多人新增 migration 前先確認最新 `head`。

```bash
docker compose run --rm migrate alembic current
docker compose run --rm migrate alembic heads
```

### 6.2 共用骨架不可重複實作

重用：

- `app/db/`：PostgreSQL Session。
- `app/storage/`：MinIO 操作。
- `app/auth/`：登入與權限。
- `app/core/`：設定、錯誤及日誌。

修改共用區域前應先通知其他組員。

### 6.3 金額及比例不可使用 float

- PostgreSQL 使用 `numeric`。
- Python 使用 `Decimal`。
- 後端統一四捨五入模式、位數及發生時點。
- 前端與 AI 傳入的計算結果只可作為參考，不得直接成為正式結果。

### 6.4 AI 輸出必須經 Schema 驗證

- Bedrock 回傳欄位必須符合 JSON Schema 或 Pydantic Schema。
- 不接受模型自由文字直接寫入資料庫欄位。
- 不存在的欄位名稱、文件 ID、宗地 ID 或表單 ID 必須拒絕。
- AI 工具輸入必須重新執行權限、狀態與 `case_id` 驗證。
- 高風險或低信心欄位必須由使用者確認。

建議欄位狀態：

```text
EXTRACTED             已擷取，尚未確認
NEEDS_CONFIRMATION    低信心或有衝突
CONFIRMED             使用者已確認
REJECTED              使用者拒絕
APPLIED               已寫入表單草稿
```

### 6.5 文件不可直接覆蓋

```text
cases/{case_id}/{category}/{document_group_id}/v{version_no}/{document_id}_{filename}
```

必須保存 `document_group_id`、`document_id`、`version_no`、`is_active`、`bucket_name` 與 `object_key`。不得保存完整 MinIO URL 或會過期的 presigned URL。

### 6.6 MinIO 與 PostgreSQL 必須保持一致

```text
驗證權限、案件及檔案
→ 產生 document_id 與 object_key
→ 上傳 MinIO
→ 取得 SHA-256、大小及 ETag
→ 寫入 PostgreSQL metadata
→ commit
```

若 metadata 寫入失敗，應補償刪除剛上傳的 MinIO 物件。

### 6.7 案件之間不可交叉引用

所有宗地、表單、文件、擷取結果、計算結果、檢核結果及產出 PDF 都必須屬於同一個 `case_id`。

### 6.8 製作前檢核與智慧審查分開

第一個子系統只做製作前機器檢核：

- 必填欄位。
- 格式與數值範圍。
- 文件缺漏。
- 公式與加總。
- 跨表基本一致性。

第二個子系統才負責：

- 法規及評價基準適用性。
- 優劣程度與修正率合理性。
- 風險分級與疑點建議。
- 人工接受、修改、退回及補件決策。

### 6.9 第一個子系統只負責寫入事件

第一個子系統應留下事件，但不必實作完整履歷搜尋與統計介面。第三個子系統負責查詢、時間軸、版本比較及分析。

### 6.10 不直接刪除正式案件

正式案件使用停用或封存。封存後不可再修改表單、宗地或文件。

---

## 7. 建議後端模組

```text
app/
├── valuation/
│   ├── router.py
│   ├── cases/
│   ├── parcels/
│   ├── requirements/       表單需求與必要文件清單
│   ├── documents/
│   ├── extraction/         PDF、OCR、文件分類與候選欄位
│   ├── forms/
│   ├── calculations/
│   ├── validations/
│   ├── reports/            固定模板 PDF 產出
│   └── events/             寫入案件事件
├── ai_assistant/
│   ├── router.py
│   ├── schemas.py
│   ├── service.py          對話與流程協調
│   ├── bedrock_client.py
│   ├── prompts/
│   ├── tools/              可供模型呼叫的受控工具
│   └── policies.py         可用工具、確認門檻及流程限制
├── review/
├── history/
├── knowledge/
├── api/
├── auth/
├── core/
├── db/
├── health/
└── storage/
```

每個業務模組原則上區分：

```text
router.py       HTTP API
schemas.py      請求與回傳格式
models.py       SQLAlchemy Model
service.py      業務規則
repository.py   PostgreSQL 查詢與寫入
```

---

## 8. AI 助理工具設計

Bedrock 模型不直接操作資料庫，而是只能呼叫 FastAPI 提供的工具。

一週 MVP 建議至少提供以下工具：

| 工具名稱 | 用途 | 是否需使用者確認 |
|---|---|---:|
| `get_case_summary` | 讀取案件目前狀態 | 否 |
| `get_form_requirements` | 取得指定表單需求 | 否 |
| `get_missing_items` | 取得缺少欄位與文件 | 否 |
| `list_case_documents` | 查看案件文件 | 否 |
| `start_document_extraction` | 啟動文件擷取 | 是 |
| `get_extracted_fields` | 取得候選欄位 | 否 |
| `apply_confirmed_fields` | 寫入已確認欄位 | 是 |
| `save_form_draft` | 儲存表單草稿 | 是 |
| `run_calculation` | 執行後端公式 | 是 |
| `run_validation` | 執行製作前檢核 | 否 |
| `generate_report_pdf` | 產生 PDF | 是，且須通過必要檢核 |

模型只負責選擇工具及組織回應；工具 Service 仍必須自行驗證權限、案件範圍、資料狀態及輸入 Schema。

---

## 9. 核心 API

API 路徑統一使用子系統前綴：

```text
/api/v1/valuation/...
/api/v1/ai-assistant/...
```

### 9.1 案件與宗地

```http
POST   /api/v1/valuation/cases
GET    /api/v1/valuation/cases
GET    /api/v1/valuation/cases/{case_id}
PATCH  /api/v1/valuation/cases/{case_id}
POST   /api/v1/valuation/cases/{case_id}/archive

POST   /api/v1/valuation/cases/{case_id}/parcels
GET    /api/v1/valuation/cases/{case_id}/parcels
PATCH  /api/v1/valuation/cases/{case_id}/parcels/{parcel_id}
```

### 9.2 表單需求與表單

```http
GET    /api/v1/valuation/form-types
GET    /api/v1/valuation/form-types/{form_type}/requirements
POST   /api/v1/valuation/cases/{case_id}/forms
GET    /api/v1/valuation/cases/{case_id}/forms/{form_id}
PATCH  /api/v1/valuation/cases/{case_id}/forms/{form_id}
POST   /api/v1/valuation/cases/{case_id}/forms/{form_id}/submit
```

需求 API 至少回傳：

```json
{
  "form_type": "F03",
  "required_fields": ["valuation_date", "benchmark_land_id"],
  "required_documents": ["land-register", "cadastral-map"],
  "optional_documents": ["photos", "attachments"],
  "calculated_fields": ["adjusted_unit_price", "benchmark_land_price"]
}
```

### 9.3 文件與擷取

```http
POST   /api/v1/valuation/cases/{case_id}/documents
GET    /api/v1/valuation/cases/{case_id}/documents
GET    /api/v1/valuation/cases/{case_id}/documents/{document_id}/download
POST   /api/v1/valuation/cases/{case_id}/documents/{document_id}/extract
GET    /api/v1/valuation/cases/{case_id}/documents/{document_id}/extraction
POST   /api/v1/valuation/cases/{case_id}/documents/{document_id}/extraction/confirm
```

候選欄位至少包含：

```json
{
  "field_name": "parcel_number",
  "extracted_value": "123-4",
  "confidence": "0.91",
  "source_page": 2,
  "source_text": "地號 123-4",
  "status": "NEEDS_CONFIRMATION"
}
```

### 9.4 計算、檢核與 PDF

```http
POST   /api/v1/valuation/cases/{case_id}/calculations
GET    /api/v1/valuation/cases/{case_id}/calculations/{calculation_id}
POST   /api/v1/valuation/cases/{case_id}/validations
GET    /api/v1/valuation/cases/{case_id}/validations/{validation_run_id}
POST   /api/v1/valuation/cases/{case_id}/reports
GET    /api/v1/valuation/cases/{case_id}/reports/{document_id}/download
```

### 9.5 AI 助理

```http
POST   /api/v1/ai-assistant/sessions
POST   /api/v1/ai-assistant/sessions/{session_id}/messages
GET    /api/v1/ai-assistant/sessions/{session_id}
GET    /api/v1/ai-assistant/sessions/{session_id}/progress
```

`messages` API 應支援模型多次工具呼叫，直到取得最終回覆或達到最大工具呼叫次數。必須設定 timeout、重試及最大循環次數，避免無限代理迴圈。

---

## 10. 一週建置順序

一週不是把正式系統全部完成，而是先完成一條可展示、可驗收的智慧製作流程。建議以一種主要表單完整串接，其他表單先完成基本 CRUD。

### Day 1：共同基線、案件、宗地及需求清單

完成：

- 確認 Docker Compose、migration、PostgreSQL、MinIO 與 Swagger。
- 案件建立、查詢、修改與封存。
- 宗地新增、查詢與修改。
- F01～F04 表單類型與需求清單 API。
- 建立 AI 工作階段資料結構。

驗收：可建立案件、加入宗地、選擇表單並取得所需欄位與文件清單。

### Day 2：文件上傳與內容擷取

完成：

- 文件上傳、列表及下載。
- 後端文件版本號與 MinIO 路徑。
- 文字型 PDF 文字擷取。
- Textract Provider 介面；有 AWS 權限時串接 Textract。
- 保存候選欄位、信心分數及來源頁碼。

驗收：上傳固定測試文件後，可以取得結構化候選欄位，且不直接污染正式表單。

### Day 3：表單草稿與 AI 助理引導

完成：

- F01～F04 建立、暫存、讀取與提交。
- 實作一種主要表單的完整欄位 Schema，建議以 F03 為主。
- Bedrock Converse API 串接。
- `get_form_requirements`、`get_missing_items`、`get_extracted_fields`、`apply_confirmed_fields` 工具。
- AI 回應顯示目前完成度、缺少資料及下一步。

驗收：使用者可透過對話選擇 F03，AI 能引導上傳資料、顯示擷取結果並在確認後寫入草稿。

### Day 4：估價計算與製作前檢核

完成：

- 一條可展示的 F03 比準地價格計算。
- 使用 `Decimal`、固定四捨五入及輸入快照。
- 必填、文件缺漏、範圍、公式及基本一致性檢核。
- `run_calculation` 與 `run_validation` AI 工具。
- AI 將檢核結果轉成修正提示。

驗收：固定資料可重複得到相同結果；故意輸入錯誤時，AI 能說明應修正的欄位。

### Day 5：PDF、事件紀錄與端到端整合

完成：

- 固定模板 PDF 產生。
- PDF 上傳 MinIO generated 路徑並寫入 metadata。
- `generate_report_pdf` AI 工具。
- 寫入主要案件事件及 request ID。
- 完成最短端到端流程。

驗收：使用者透過 AI 完成資料確認、計算、檢核與 PDF 下載。

### Day 6～7：緩衝、測試與展示資料

若一週包含週末，僅用於：

- 修復整合問題。
- 補齊錯誤處理及權限測試。
- 準備一組穩定的展示案件與文件。
- 測試 Bedrock 或 Textract 暫時不可用時的降級提示。
- 演練完整展示流程。

不要在此時新增新的大型功能。

---

## 11. 一週 MVP 範圍

### 11.1 必須完成

1. 案件新增、查詢、修改及封存。
2. 宗地新增、查詢及修改。
3. F01～F04 的表單需求清單。
4. F01～F04 基本建立、暫存、讀取及提交。
5. 一種主要表單的完整 AI 製作流程，建議 F03。
6. 文件上傳、列表、下載及後端版本保留。
7. 至少支援文字型 PDF 擷取；Textract 能用則加入掃描件 OCR。
8. 擷取候選欄位、信心分數、來源及使用者確認。
9. Bedrock 對話引導與受控工具呼叫。
10. 一條可重現的估價計算。
11. 必填、文件缺漏、範圍及計算錯誤檢核。
12. 主要操作事件紀錄。
13. 一份固定模板、可下載的 PDF 估價書。
14. 完整端到端驗收。

### 11.2 有時間再完成

- F01～F04 全部複雜公式與完整自動帶入。
- 多種版型的 OCR 最佳化。
- Bedrock Knowledge Bases 法規檢索；此部分更適合第四個子系統。
- 完整文件版本比較及恢復介面。
- 多人同時編輯與完整樂觀鎖定。
- 完整欄位級稽核介面。
- 完整規則版本管理後台。
- 正式版 PDF 視覺樣式及多版本比較。
- AI 自動選擇比較案例或判定修正率。
- 第二個子系統的審查風險分級與人工決策。

---

## 12. 估價計算要求

展示流程至少要做到：

```text
讀取已確認的比較案例
→ 套用條件修正
→ 計算調整後單價
→ 計算比準地價格
→ 保存輸入快照、公式版本、步驟及結果
```

規則：

- 計算寫在 Service 或 Domain 層，不寫在 Router、Prompt 或 Vue。
- 使用 `Decimal`。
- 保存規則版本與輸入快照。
- 不只保存最後價格。
- 同一輸入與規則版本必須得到相同結果。
- 來源資料變更後建立新結果，不覆蓋舊結果。
- AI 只能呼叫計算工具與解釋已回傳結果。

---

## 13. 製作前機器檢核

一週 MVP 先做規則型檢核：

- 必填欄位缺漏。
- 面積、價格及修正率範圍。
- 必要文件缺漏。
- 案件與表單資料一致性。
- F01～F04 基本跨表一致性。
- 計算加總正確性。
- MinIO 文件是否存在。

每筆檢核結果至少保存：

- `case_id`
- `form_id` 或 `field_path`
- `rule_code` 與 `rule_version`
- `actual_value`
- `expected_value`
- `severity`
- `message`
- `triggered_by_user_id`
- `request_id`
- `created_at`

`ERROR` 尚未修正時不得產生正式完成版 PDF；`WARNING` 可由使用者確認後繼續，但應留事件紀錄。

---

## 14. PDF 產生原則

正確流程：

```text
AI 助理確認使用者要產生估價書
→ 後端再次檢查權限、案件狀態與最新檢核結果
→ 讀取案件、宗地、已確認表單及最新計算結果
→ 以固定模板產生 PDF
→ 上傳 MinIO generated 路徑
→ 寫入文件 metadata
→ 寫入 REPORT_GENERATED 事件
→ 回傳下載資訊給 AI 助理
```

產出位置：

```text
cases/{case_id}/generated/{document_group_id}/v{version_no}/{document_id}_{filename}
```

AI 可以協助組合「摘要、缺件說明或計算解釋」等文字，但案件編號、地號、面積、價格、公式結果及表格資料必須直接取自後端已確認資料，不得由模型重新生成。

---

## 15. API、權限與錯誤要求

| 情況 | HTTP Status |
|---|---:|
| 未登入 | `401` |
| 已登入但沒有權限 | `403` |
| 找不到案件或資源 | `404` |
| 唯一資料重複或狀態衝突 | `409` |
| 輸入欄位不合法 | `422` |
| Bedrock／Textract 暫時不可用 | `503` |

角色原則：

- 估價人員：可建立及編輯本人有權處理的案件。
- 審查人員：可讀取並進入第二個子系統，不直接改寫估價內容。
- 稽查人員：依權限查閱案件及履歷，不直接修改。

AI 工具呼叫不得因為「是系統自己呼叫」就略過使用者身分與權限檢查。

---

## 16. 測試與驗收

### 16.1 單元測試

- Decimal 計算及四捨五入。
- object key 產生。
- 文件版本號。
- 權限判斷。
- 表單欄位驗證。
- 必填與計算檢核規則。
- AI 工具輸入 Schema。
- PDF 資料組裝不使用未確認欄位。

### 16.2 API 與整合測試

- `401`、`403`、`404`、`409`、`422`、`503`。
- 跨案件引用被拒絕。
- 封存案件不能編輯。
- 未確認擷取值不能直接成為正式值。
- 寫入失敗會 rollback。
- MinIO 上傳失敗或 metadata 失敗有正確補償處理。
- AI 不可呼叫未允許的工具。
- AI 重複工具呼叫不會重複建立正式結果。
- 回應具有一致 request ID 與錯誤格式。

### 16.3 最短端到端驗收

1. 估價人員登入。
2. 建立案件並新增宗地。
3. 開啟 AI 助理並選擇 F03。
4. AI 顯示必要欄位及文件。
5. 使用者上傳土地登記資料與地籍圖。
6. 系統擷取候選欄位。
7. AI 顯示擷取結果，使用者確認或修正。
8. AI 將已確認資料寫入表單草稿。
9. 使用者補充無法擷取的欄位。
10. AI 呼叫後端估價計算。
11. AI 呼叫製作前機器檢核。
12. 使用者依提示修正錯誤並重新檢核。
13. AI 呼叫固定模板 PDF 產生工具。
14. 使用者下載並正常開啟 PDF。
15. 確認 PostgreSQL、MinIO、計算結果、檢核與事件紀錄一致。

這條流程完整跑通，才算完成第一個子系統的一週競賽 MVP。

---

## 17. 一週開發風險與降級方案

| 風險 | 降級方案 |
|---|---|
| AWS 憑證或區域尚未提供 | 保留 Provider 介面，使用固定 Mock 模型回應完成工具流程 |
| Textract 無法使用 | 只支援文字型 PDF，掃描件提示人工輸入 |
| Bedrock 回應格式不穩 | 使用 tool schema、低 temperature、Pydantic 驗證及一次修復重試 |
| 四種表單來不及完整串接 | F03 完整展示，其餘完成基本 CRUD 與需求清單 |
| PDF 正式版面耗時 | 使用固定簡化模板，但欄位、金額與下載流程必須正確 |
| AI 誤填資料 | 候選欄位先確認，計算與 PDF 只讀 CONFIRMED 資料 |
| 外部 AI 服務延遲 | 設定 timeout、有限次重試並提供手動填寫入口 |

降級模式仍必須清楚標示，不可假裝已由真實 AI 或 OCR 完成。

---

## 18. 每次提交前檢查

- [ ] 位於正確 feature branch。
- [ ] 未提交密碼、AWS Key 或 `.env`。
- [ ] 未修改已執行的舊 migration。
- [ ] 新 API 已掛到統一 Router。
- [ ] API 有 Pydantic 輸入與回傳 Schema。
- [ ] API 與 AI 工具都有登入、角色及案件權限檢查。
- [ ] 金額與比例使用 Decimal。
- [ ] AI 結構化輸出已驗證。
- [ ] 未確認欄位不會進入正式計算或 PDF。
- [ ] 跨案件引用會被拒絕。
- [ ] 失敗交易會 rollback。
- [ ] MinIO 測試物件已清除。
- [ ] Bedrock／Textract 錯誤有 timeout 與降級提示。
- [ ] 新增功能有測試。
- [ ] `/health/ready` 仍正常。
- [ ] Swagger 可以操作新 API。
- [ ] README 或 UPDATE 已記錄重要變更。

建議 Commit Message：

```text
feat(valuation): add case requirements workflow
feat(extraction): add document field extraction pipeline
feat(ai-assistant): add bedrock guided valuation workflow
feat(calculation): add deterministic benchmark calculation
feat(validation): add pre-submission validation rules
feat(reports): add valuation PDF generation
test(valuation): add AI-assisted workflow integration test
docs(valuation): update one-week AI backend guide
```

---

## 19. 最終完成定義

第一個子系統不是「接上 Bedrock」或「API 數量很多」就算完成，而是必須證明：

> 估價人員登入並建立案件後，能由 AI 助理根據表單類型說明所需資料，逐項引導上傳文件或填寫基本資料，整理文件擷取結果供使用者確認，再由後端完成可重現的估價計算與製作前檢核；修正錯誤後，AI 助理可呼叫固定模板服務產生並提供 PDF 估價書，同時確保 PostgreSQL、MinIO、權限、計算與事件紀錄一致。

一週內應優先完成這條最短完整流程，不應同時追求四種表單的所有複雜規則、完整 OCR 泛化能力、完整法規 RAG 或正式版 PDF 美術排版。

---

## 20. AWS 官方參考

- [Amazon Bedrock Converse API](https://docs.aws.amazon.com/bedrock/latest/APIReference/API_runtime_Converse.html)
- [Amazon Bedrock client-side tool use](https://docs.aws.amazon.com/bedrock/latest/userguide/tool-use-client-side.html)
- [Amazon Textract AnalyzeDocument](https://docs.aws.amazon.com/textract/latest/dg/analyzing-document-text.html)
- [Amazon Textract 表單 Key-Value 擷取](https://docs.aws.amazon.com/textract/latest/dg/how-it-works-kvp.html)
- [Amazon Textract 最佳實務](https://docs.aws.amazon.com/textract/latest/dg/textract-best-practices.html)

